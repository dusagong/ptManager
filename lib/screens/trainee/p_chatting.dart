import 'package:flutter/material.dart';

class P_Chat extends StatelessWidget {
  const P_Chat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}